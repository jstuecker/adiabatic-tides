[pytest]
markers =
    slow: marks tests as slow taking > 3s per option (deselect with '-m "not slow"')
    plot: tests that generate plots
    veryslow: marks tests as very slow taken > 10s per option (deselect with '-m "not veryslow"')

addopts = -m "not veryslow"
; addopts = -s -m "not veryslow"